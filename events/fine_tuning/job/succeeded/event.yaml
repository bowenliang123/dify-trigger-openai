identity:
  name: "fine_tuning_job_succeeded"
  author: "bowenliang123"
  label:
    en_US: "Finetuning Job Succeeded Event"
    zh_Hans: "Finetuning Job Succeeded 事件"

description:
  en_US: "Finetuning Job Succeeded Event"
  zh_Hans: "Finetuning Job Succeeded 事件"

output_schema:
  type: object
  properties:
    data:
      type: object
      description: "Event data payload."
      properties:
        id:
          type: string
          description: "The unique ID of the fine-tuning job."
    created_at:
      type: number
      description: "The Unix timestamp (in seconds) of when the model response was completed."
    id:
      type: string
      description: "The unique ID of the event."
    object:
      type: string
      description: "The object of the event. Always event."
    type:
      type: string
      description: "The type of the event."

extra:
  python:
    source: events/fine_tuning/job/succeeded/event.py
