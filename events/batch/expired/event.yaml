identity:
  name: "batch_expired"
  author: "bowenliang123"
  label:
    en_US: "Batch Expired Event"
    zh_Hans: "Batch Expired 事件"

description:
  en_US: "Batch Expired Event"
  zh_Hans: "Batch Expired 事件"

output_schema:
  type: object
  properties:
    data:
      type: object
      description: "Event data payload."
      properties:
        id:
          type: string
          description: "The unique ID of the batch API request."
    created_at:
      type: number
      description: "The Unix timestamp (in seconds) of when the model response was completed."
    id:
      type: string
      description: "The unique ID of the event."
    object:
      type: string
      description: "The object of the event. Always event."
    type:
      type: string
      description: "The type of the event."

extra:
  python:
    source: events/batch/expired/event.py
